# Secrets and local credentials
.env
credentials.json
token.json
clients.local.json

# Local Python environment
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.Python

# Runtime state and locks
*.lock
live_history_state.json
live_state.json

# Local scheduler wrappers (may source secrets)
run_live.sh
run_backfill.sh

# Decision / validation logs (may contain email metadata)
*.log
*.jsonl

# Migration / cleanup snapshots
*snapshot*
migration-run-batch-*.log

# Editor / OS
.DS_Store
.idea/
.vscode/
*.bak
*.swp
